#disabled: We currently haven't decided how to handle annotations, yet
#output: Something

class Foo {
	static val VALUE = "Something";

	this() {}
}

[Test(Foo.VALUE)]
class Bar {
	this() {}
}

var bar = new Bar();
println(testAnnotationValue(bar));
