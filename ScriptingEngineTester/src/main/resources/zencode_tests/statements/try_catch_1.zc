#disabled: Not part of the refactor
#disabled: Methods currently don't have a header which allows throwing of exceptions
#output: TestException
#output: test

import testsupport.TestException;

try {
	throw new TestException("test");
} catch ex {
	println(typeof(ex));
	println(ex.message);
}
