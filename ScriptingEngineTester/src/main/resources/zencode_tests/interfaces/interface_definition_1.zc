#output: hello

interface Test {
	print(): void;
}

class TestClass {
	implements Test {
		print() {
			println("hello");
		}
	}
}

// ToDo: Currently fails with an IncompatibleClassChangeError due to calling invokeInterface
//  on "script_tests.interfaces.TestClass.println()" instead of "script_tests.interfaces.Test.print()"
new TestClass().print();
