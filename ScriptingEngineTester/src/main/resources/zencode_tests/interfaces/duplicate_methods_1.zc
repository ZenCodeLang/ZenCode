interface IntA {
     asString(): string;
}

class MyClass {
  asString(): string => "Some value";
  public implements IntA { asString() => asString(); }
}

val a = new MyClass();
println(a.asString());
