#disabled: not part of the refactor, requires more thought on whether this should be an override without base or an invalid cast string->void
#error: 9:OVERRIDE_WITHOUT_BASE

interface Test {
	print(): void;
}

class TestClass {
	implements Test {
		print() {
			return "hello";
		}
	}
}

new TestClass().print();
