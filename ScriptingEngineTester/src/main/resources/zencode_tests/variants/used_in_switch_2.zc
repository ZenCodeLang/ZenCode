#disabled: Disabled since currently the parser fails with a "Type Expected" at the closing }
#output: Empty

public variant Optional<T> {
	Present(T),
	Empty;
}

var opt as Optional<string> = Empty;

switch(opt) {
	case Present(value):
		println(value);
		break;
	case Empty:
		println("Empty");
		break;
	}
}
