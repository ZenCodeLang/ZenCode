#disabled: Not part of the refactor, we currently don't compile ValueEnums properly
#output: X
#output: Y
#output: Z

enum MyEnum: string {
	A = "X",
	B = "Y",
	C = "Z"
}

println(MyEnum.A);
println(MyEnum.B);
println(MyEnum.C);
