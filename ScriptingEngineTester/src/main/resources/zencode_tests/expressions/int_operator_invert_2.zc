#output: int
#output: -43
#output: int
#output: 42

println(typeof(~42));
println(~42); // ToDo: Type inference does not work properly here: "No INVERT operator on type STRING"

println(typeof(~(~42)));
println(~(~42));
