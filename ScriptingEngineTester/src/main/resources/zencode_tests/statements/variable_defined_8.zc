#disabled: Not part of the refactor
#disabled: These checks haven't been implemented yet
#output: 1

import testsupport.TestException;

val a as int;
try {
	if false
		throw new TestException("test");
	a = 1;
} catch {
	a = 2;
}

println(a);
