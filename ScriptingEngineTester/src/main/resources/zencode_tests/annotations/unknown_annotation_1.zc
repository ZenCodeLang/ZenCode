#disabled: We currently haven't decided how to handle annotations, yet
#error: 3:UNKNOWN_ANNOTATION

[Unknown("Name")]
class Foo {
	this() {}
}

var foo = new Foo();
println(testAnnotationValue(foo));
