#disabled: Not part of the refactor
#output: 8

public class Stuff {
	public val x as int : get;

	this(x: int) {
		this.x = x;
	}

	asSupplier(): function() as function() as int {
		return () => () => this.x;
	}
}

println(new Stuff(4).asSupplier()()());
