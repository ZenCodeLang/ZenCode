#output: usize
#output: 42
#output: usize
#output: 4294967253
#output: usize
#output: 42

var i = 42 as usize;
var inverted = ~i;
var invertedAgain = ~inverted;

println(typeof(i));
println(i);

println(typeof(inverted));
println(inverted);

println(typeof(invertedAgain));
println(invertedAgain);
