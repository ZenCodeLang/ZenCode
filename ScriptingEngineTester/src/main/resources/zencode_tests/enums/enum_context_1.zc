#output: B
#output: C

public enum MyEnum {
	A, B, C;

	this(){}
}

val x: MyEnum = B;
var y: MyEnum; // TODO: should also work with val since this is the first time it's set
y = C;
println(x.name);
println(y.name);
