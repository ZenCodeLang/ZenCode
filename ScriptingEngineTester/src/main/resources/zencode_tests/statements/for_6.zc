#output: (1|1)
#output: (1|3)
#output: (3|1)
#output: (3|3)

val nums = [1, 2, 3, 4];

for x in nums {
    if (x % 2 == 0) continue;
    for y in nums {
        if (y % 2 == 0) continue;

        println("(" + x + "|" + y + ")");
    }
}
