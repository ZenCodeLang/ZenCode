#output: 6.06.0

expand double {
	foo(): double => this * (2 as int);
}

expand double {
	bar(): string => this as string;
}

expand string {
	baz(): string => this + this;
}

println((3.0).foo().bar().baz());
