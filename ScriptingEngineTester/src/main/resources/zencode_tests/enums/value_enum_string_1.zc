#disabled: Not part of the refactor, we currently don't compile ValueEnums properly
#output: X
#output: Y
#output: C

enum MyEnum: string {
	A = "X",
	B = "Y",
	C
}

println(MyEnum.A);
println(MyEnum.B);
println(MyEnum.C);
