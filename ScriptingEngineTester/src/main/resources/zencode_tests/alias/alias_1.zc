#disabled: Not necessary for CraftTweaker (so far)
#output: Two is Foo
#output: Two is int

alias Foo as int;

val two : Foo = 2;

if (two is Foo) {
	println("Two is Foo");
}

if (two is int) {
	println("Two is int");
}